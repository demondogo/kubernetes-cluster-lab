---
- name: Generate netplan configuration
  ansible.builtin.command:
    cmd: netplan generate
  changed_when: false
  listen: Apply netplan

- name: Apply netplan
  ansible.builtin.command:
    cmd: netplan apply
  changed_when: true
