---
all:
  children:

    control_plane:
      hosts:
        server:
          ansible_host: 192.168.56.20
          ansible_user: vagrant
          ansible_ssh_private_key_file: "{{ inventory_dir }}/../../.vagrant/machines/server/virtualbox/private_key"

    workers:
      hosts:
        node-0:
          ansible_host: 192.168.56.50
          ansible_user: vagrant
          ansible_ssh_private_key_file: "{{ inventory_dir }}/../../.vagrant/machines/node-0/virtualbox/private_key"

        node-1:
          ansible_host: 192.168.56.60
          ansible_user: vagrant
          ansible_ssh_private_key_file: "{{ inventory_dir }}/../../.vagrant/machines/node-1/virtualbox/private_key"

    management:
      hosts:
        jumpbox:
          ansible_host: 192.168.56.10
          ansible_user: vagrant
          ansible_ssh_private_key_file: "{{ inventory_dir }}/../../.vagrant/machines/jumpbox/virtualbox/private_key"